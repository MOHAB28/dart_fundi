import 'classes.dart';

void main() {
  WeekDays days = WeekDays.monday;

  print(days.dayShorts);
}

/*

- Ticket Booking System
Create a class `User` with `name` and `email`.
- Create an enum `TicketType` with values `Regular`, `VIP`.
- Create an abstract class `Ticket` with a method `getPrice()`.
- Create classes `RegularTicket` and `VIPTicket` that implement `Ticket`.
- Create a class `BookingSystem` that books tickets and calculates total price.

*/
